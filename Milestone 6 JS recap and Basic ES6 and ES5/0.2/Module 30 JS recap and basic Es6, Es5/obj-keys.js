const person = {
    name: 'Jon',
    age: 30,
    occupation: 'programmer',
    phone: 015568911,
}


const key = Object.keys(person);
const value = Object.values(person);
const entity = Object.entries(person);
console.log(key);
console.log(value);
console.log(entity);