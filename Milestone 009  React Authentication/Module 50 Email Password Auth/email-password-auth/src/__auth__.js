/**
 * 1. login === sign in ===> you are an old user. You already have the credentials (email and password) to login.
 * 2. register === sign up === create account => you are a brand new user. You have never used the website before. you do not have the email and password that have access.
 */